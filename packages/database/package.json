{
  "name": "database",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Camada de banco de dados compartilhada",
  "scripts": {
    "db:migrate": "tsx src/migrations/run.ts",
    "db:seed": "tsx src/migrations/seed.ts",
    "build": "tsc",
    "check": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "dependencies": {
    "pg": "^8.11.3",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "@types/pg": "^8.11.4",
    "prettier": "^3.6.2",
    "tsx": "^4.19.1",
    "typescript": "^5.7.2"
  }
}
